import React from 'react'
import SideNav from '../SideNav/SideNav'

function Layout() {
    return (
        <section class="d-flex">
            <SideNav />
        </section>
    )
}

export default Layout
